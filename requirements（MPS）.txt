
python>=3.8,<3.11  # 适配PyTorch/transformers主流版本，3.11+存在部分库兼容问题

# ==============================================================================
# 【核心依赖】（必须安装）
# ==============================================================================
# 深度学习框架
torch>=2.0.0        # 支持MPS(CUDA)加速，Apple Silicon/NVIDIA GPU适配
torchvision>=0.15.0 # 图像预处理组件，与torch版本强绑定

# 预训练模型相关
transformers>=4.35.0        # 加载DeBERTa/CLIP/ResNet等模型，包含Tokenizer/优化器调度器
huggingface-hub>=0.19.4     # 从HF Hub下载预训练模型，管理缓存与版本

# 数据处理核心
pandas>=2.0.0       # 读取CSV/Excel数据、标签统计、数据清洗（代码核心数据载体）
numpy>=1.24.0       # 数值计算、数组操作、矩阵运算（所有模型的基础数据结构）
scikit-learn>=1.2.0 # 数据分割、指标计算（F1/Accuracy/混淆矩阵/交叉验证）
scipy>=1.10.1       # 高级数值计算（文本长度统计/概率分布/优化算法）

# 图像处理核心
Pillow>=9.5.0       # 图像读取、格式转换、增强处理（CLIP/图像模型必需，注意首字母大写）

# 工具类核心
tqdm>=4.65.0        # 训练/验证进度条显示（直观监控训练过程）
nltk>=3.8.1         # 文本分词、停用词处理（需手动下载punkt/stopwords数据集）
typing-extensions>=4.5.0 # 类型注解增强（适配Python3.8+，避免代码类型检查报错）

# ==============================================================================
（建议安装）
# ==============================================================================
# 可视化增强
matplotlib>=3.7.0   # 绘制训练曲线、标签分布、混淆矩阵（结果分析必需）
seaborn>=0.12.2     # 可视化美化（混淆矩阵/热力图更易读）

# 配置与性能优化
json5>=0.9.14       # 支持注释的JSON配置文件读取（比原生json更灵活）
accelerate>=0.24.1  # 多GPU训练加速、分布式训练（单GPU也可装，无性能损耗）
openai-clip>=1.0.1  # CLIP模型专用依赖（若用CLIP则必需，不用则可选）

# 日志与调试
loguru>=0.7.2       # 更友好的日志输出（替代print，便于问题排查）
rich>=13.3.4        # 终端彩色输出、表格展示（提升调试体验）

# ==============================================================================
# 【可选依赖】
# ==============================================================================
# GPU/分布式训练
# deepspeed>=0.10.0  
# fairscale>=0.4.13  # 模型并行/数据并行（仅多GPU集群需要）

# 数据格式扩展（仅用到对应格式时安装）
openpyxl>=3.1.2     # 读取Excel(.xlsx)文件（仅用CSV则无需安装）
xlrd>=2.0.1         # 读取旧版Excel(.xls)文件（极少使用）
pyarrow>=12.0.1     # 高效读取Parquet格式数据（大数据场景用）

# 额外功能扩展（非核心）
jieba>=0.42.1       # 中文分词增强（若处理中文文本建议装）
beautifulsoup4>=4.12.2 # 网页文本解析（仅爬取数据时需要）
requests>=2.31.0    # HTTP请求（仅调用外部API/下载数据时需要）

# 测试与验证（开发/调试阶段用）
pytest>=7.4.0       # 单元测试框架（仅开发调试时需要）
coverage>=7.2.7     # 测试覆盖率统计（仅开发调试时需要）

# random/itertools: 随机数/迭代工具